# vite-plugin-agent-skills

Vite plugin for importing Agent Skills from `SKILL.md` files.

```ts
import { agentSkills } from "vite-plugin-agent-skills";

export default {
  plugins: [agentSkills()],
};
```
